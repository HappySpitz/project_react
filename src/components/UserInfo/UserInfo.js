import React from 'react';

import css from './UserInfo.module.css';

const UserInfo = () => {
    return (
        <div className={css.UserInfo}>
           <div className={css.UserInfoText}>U</div>
        </div>
    );
};

export {
    UserInfo
};